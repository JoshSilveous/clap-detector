/**
 * amount of time to wait after detecting a first clap, before beginning to detect the second clap
 */
export const CLAP_THRESHOLD = 50
/**
 * amount of time to wait to detect the second clap after detecting the first
 */
export const CLAP_TIMEOUT = 1000
/**
 * adjust to make volume threshold more or less sensitive
 */
export const VOLUME_THRESHOLD = 15000
